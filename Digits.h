#ifndef DIGITS_H
#define DIGITS_H

#include <QObject>

class Digits : public QObject
{
    Q_OBJECT
public:
    explicit Digits(QObject *parent = nullptr);

signals:

public slots:
};

#endif // DIGITS_H